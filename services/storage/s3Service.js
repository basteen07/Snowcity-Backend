const {
  s3,
  uploadBuffer,
  uploadFile,
  removeObject,
  getSignedObjectUrl,
  BUCKET,
  REGION,
} = require('../../config/s3');

module.exports = {
  s3,
  uploadBuffer,
  uploadFile,
  removeObject,
  getSignedObjectUrl,
  BUCKET,
  REGION,
};